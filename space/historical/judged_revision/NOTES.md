# Historical rejected baseline

This directory preserves exact copies of the three judged text files that had
to change at their canonical paths to expose current verification:
`README.md`, `logbook.json`, and `pages/index.md`. Their SHA-256 values match
Space revision `9449686999e15049fa1e258517852b9f2a00bda1`.

The original `pages/overview/page.md`, frontend files, SVG, and PNG assets
remain unchanged at their original paths. `original_manifest.sha256` records
all 13 protected paths and hashes.
